import java.util.ArrayList;
import java.util.Scanner;
public class idkclass {
    public static void main(String args[]){

        private int intPriority;
        private String stringPriority; 
        
        Scanner input = new Scanner(System.in);
        
        System.out.println("Enter description: ");
        String description = input.nextLine();
        System.out.println("Enter Day, Month, Year separated by spaces: ");
        String date = input.nextLine();
        System.out.println("Enter priority as [i]nt or [s]tring? : ");
        String priorityType = input.nextLine();
        if(priorityType == "i")
        {
          System.out.println("Enter priority as int: ");
        }
        else{
            System.out.println("Enter that shit as a string u fuk: ");
        }
        
        //WE GON SPLIT SHIT.
        String[] tokens;
        for(int i = 0; i < 3; i++){
            tokens[i] = date.split(" ");
        }
        //So basically what this is doing is its tokenizing based on " ". Anything before a " " will be put into an array.
        //NOT SURE IF THIS WORKS THOUGH, remember its been like a year since I java'd. But this is basically the idea. 
        //You can use tokenize in the same way too.
        
        //After you get your game tokens, you know that because you input date in order of day, month, year, you gon get
        //your tokens in that order too. so
        int Day = token[0];
        int Month = token[1];
        int Year = token[2];
        
        //Now put this shit in vvvvvvvvvv down there in the V 


        ArrayList<ToDoItem> ListShit = new ArrayList<ToDoItem>(); 
        ToDoItem item = new ToDoItem();
        ListShit.add(new item(description, date, whateverwahtwhthwat)); //Change this to your actual inputs
    }
}
  
  public class ToDoItem {
      private String description;
      private String date; // going to have to tokenize this at some point. 
      private String priorityType;
      //etc
      public ToDoItem(String desc, String d8, String priorityT.... etc etc){
        description = desc; 
        date = d8;
        priorityType = priorityT; 
        //Obviously, you can make the input names whatever you want. To distinguish them, I changed their names so you could see what was goin on. 
      }
      
  }
